#ifndef PALINDROME_H
#define PALINDROME_H

/*
 * is_palindrome:
 *   Receives a string.
 *   Returns 1 if the string is a palindrome (ignoring spaces), 0 otherwise.
 */
int is_palindrome(char s[]);

#endif /* PALINDROME_H */



