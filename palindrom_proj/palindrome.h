#ifndef PALINDROME_H
#define PALINDROME_H

#define MAX_STRING_LENGTH 80  /* Maximum allowed string length */
/*
 * is_palindrome:
 *   Receives a string `s` and checks if it is a palindrome, ignoring spaces.
 *   It returns 1 if the string is a palindrome, 0 otherwise.
 *
 * Parameters:
 *   char s[]: The string to check.
 *
 * Returns:
 *   int: 1 if the string is a palindrome, 0 otherwise.
 */
int is_palindrome(char s[]);

#endif /* PALINDROME_H */


